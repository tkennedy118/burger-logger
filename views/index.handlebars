<div class="container">
    <h1 class="text-center mb-2">Eat Your Burgers!</h1>

    <div class="row">
        <div class="col-sm-6">
            <h2>Not Eaten</h2>
            <u1>
                {{#each burgers}}
                    {{#unless devoured}}
                        {{> burger-block devoured=true}}
                    {{/unless}}
                {{/each}}
            </u1>
        </div>

        <div class="col-sm-6">
            <h2>Eaten</h2>
            <u1>
                {{#each burgers}}
                    {{#if devoured}}
                        {{> burger-block}}
                    {{/if}}
                {{/each}}
            </u1>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <form id="add-burger-form">
                <div class="form-group">
                    <label for "burger-textarea">Add a burger here</label>
                    <textarea class="form-control" id="burger-textarea" name="burger-textarea" rows="3"></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Add Burger</button>
            </form>
        </div>
    </div>

</div>